server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/complitech_api_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      format_sql: true
      hbm2ddl.auto: validate
  mvc:
    format:
      date: iso

jwt_secret: SECRET

api:
  version: v1
  description: This API is a test task for Complitech.

date_for_stop_app: 2022-06-26 10:44:30
fixedRate: PT10M
